# include "main.h"
# include <stdio.h>
# include <string.h>

/**
* _strlen - prints out the lenght of a string
* @s: the string to be measured
* Return: lenght of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
}

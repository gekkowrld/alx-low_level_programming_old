# include "main.h"
# include <stdio.h>

/**
* mul - multiplies to numbers
* @a: first number to be multiplied
* @b: second number to be multiplied
* Return: 0
*/
int mul(int a, int b)
{
	return (a * b);
}
